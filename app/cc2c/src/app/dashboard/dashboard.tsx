'use client';

import React from 'react';
import { Grid } from '@mui/material';
import { useProtectedRoute } from '@local/lib/useProtectedRoute';

interface Props {}

export function Dashboard({}: Props) {
    return <Grid container>Dashboard</Grid>;
}
