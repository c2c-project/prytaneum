mutation Login($input: LoginForm!) {
    login(input: $input) {
        id
        firstName
        lastName
        email
        avatar
    }
}

mutation Register($input: RegistrationForm!) {
    register(input: $input) {
        id
        firstName
        lastName
        email
        avatar
    }
}